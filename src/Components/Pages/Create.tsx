import React, { Fragment } from 'react';

import Create from '../Create/Create';

const CreatePage = () => {
  return (
    <Fragment>
      <Create/>
    </Fragment>
  );
};

export default CreatePage;